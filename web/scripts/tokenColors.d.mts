/** Types for the build-time token reader (see `tokenColors.mjs`). */
export declare function tokenColor(name: string): string
export declare function brandColors(): {
  accent: string
  onAccent: string
  background: string
}
